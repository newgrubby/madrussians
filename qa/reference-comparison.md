# Reference comparison

The build retains Forest Joy’s full-viewport image opening, side index, extreme editorial type scale, three-point intro, vertical route rail, media/typography overlap and slow horizontal archive rhythm. Mobile intentionally departs from desktop layering: side navigation becomes a full-screen menu and rails remain naturally scrollable to avoid scroll traps. The form and business facts are more explicit than the reference because the MADRUSSIANS adaptation must support conversion and factual trust.
